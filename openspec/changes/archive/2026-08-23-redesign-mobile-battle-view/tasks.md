## 1. 共享回放表现模型

- [x] 1.1 从 `BattlePage` 提取桌面和紧凑布局共用的 `BattleViewModel`，统一提供当前回合、双方状态、可见事件、炸弹状态、统计、已揭示回合和回放命令。
- [x] 1.2 保持回放计时器、`roundIndex`、`visibleEventCount`、播放状态和最终揭示状态由 `BattlePage` 单点持有，防止布局组件重复推进回放。
- [x] 1.3 增加不依赖第三方运行时包的媒体查询 Hook，在 1024px 断点选择桌面或紧凑布局，并验证调整窗口和旋转设备不重置回放。

## 2. 雷达统一坐标渲染

- [x] 2.1 将 `MapView` 改为基于 1024x984 `viewBox` 的响应式 SVG，使 Dust2 底图与击杀、下包、拆包和爆炸标记位于同一坐标空间。
- [x] 2.2 实现 `0..1` 服务端坐标到 SVG 坐标的纯函数转换，对非有限或越界坐标跳过渲染并提供开发态诊断，不修改服务器事件数据。
- [x] 2.3 为中心、边界、击杀和炸弹事件补充坐标转换及可见事件过滤测试，确认未来事件和其他回合标记不会提前显示。

## 3. 桌面布局兼容

- [x] 3.1 将现有三栏比赛界面整理为 `DesktopBattleLayout`，接入共享 `BattleViewModel`，保留比分、阵容、雷达、炸弹、回合导航、实时播报和统计入口。
- [x] 3.2 移除桌面布局对移动端隐藏阵容规则的依赖，并回归验证 1024px、1440px 和 1920px 宽度下无页面级横向滚动、遮挡或功能丢失。

## 4. 移动端比赛舞台

- [x] 4.1 实现 `CompactBattleLayout` 的紧凑页头、比分/阵营/回合摘要和至少 44px 的暂停、继续、跳过比赛触控操作。
- [x] 4.2 实现紧凑回合导航，只显示已揭示回合并将横向滚动限制在导航内部；选择历史回合时保持现有暂停和回看语义。
- [x] 4.3 实现连续的移动比赛舞台，将炸弹状态条、响应式雷达、双方五人存活状态条和最近事件按顺序组合，并根据当前回合 `team_t_id` / `team_ct_id` 映射阵营颜色。
- [x] 4.4 实现移动端结构化紧凑事件格式，覆盖击杀、伤害、炸弹和回合结果，同时保留现有完整中文事件格式和事件顺序。
- [x] 4.5 实现“实时战况 / 阵容 / 数据统计”页签；阵容支持切换双方五人列表，统计复用现有当前 K/D/A 和比赛结束统计，切换页签不得改变回放进度。
- [x] 4.6 调整 BattlePage 移动样式为自然纵向滚动、`100dvh` 和安全区间距，消除固定高度裁切、页面级横向溢出以及超大桌面字号在手机端的继承。

## 5. 前端自动化与视觉验证

- [x] 5.1 扩展现有 Node 测试，覆盖共享表现模型、存活状态只随可见事件更新、阵营换边映射和紧凑事件格式化。
- [x] 5.2 增加 `@playwright/test` 开发依赖和可重复的 BattlePage 战报夹具，建立不依赖真实随机比赛结果的浏览器端测试入口。
- [x] 5.3 增加 390x844 竖屏和 844x390 横屏端到端测试，覆盖播放/暂停、选择已揭示回合、跳过比赛、三个辅助页签和无页面级横向溢出。
- [x] 5.4 增加 390x844、1024x768、1440x900 的雷达截图和关键像素/元素矩形断言，确认中心及已知事件标记在窗口缩放后仍与底图对齐。
- [x] 5.5 增加桌面端回归用例，确认两侧阵容、炸弹状态、实时播报和统计仍可访问，并检查移动端不会同时挂载桌面交互组件。
- [x] 5.6 在 `client` 中运行 `npm test`、Playwright 测试、`npm run lint` 和 `npm run build`，修复所有类型、行为和构建问题。

## 6. 后端兼容与本地交付验证

- [x] 6.1 确认实现差异不包含 Nakama RPC、Match Handler、Storage、数据库或 Luban 配表变更，并通过现有战报契约测试验证客户端未要求新的服务器字段。
- [x] 6.2 使用项目规定的 Docker 构建流程运行 `server\build.bat`，确认 Nakama Go Plugin 在未改协议的情况下仍可编译。
- [x] 6.3 无缓存重建并重新创建本地 `frontend` Docker 服务，在 `http://localhost:3000/battle` 使用手机和桌面视口完成最终冒烟与截图检查。
